build:
  docker:
    server_app: Dockerfile
  config:
    BUNDLE_INSTALL_ARGS: --jobs 10 --retry=3 --without development test
    RAILS_ENV: production
run:
  server_app: bundle exec puma -C config/puma.rb
  worker:
    command:
      - bundle exec rake rails s
    image: web