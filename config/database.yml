default: &default
  adapter: postgresql
  encoding: unicode
  host: db
  username: <%= Rails.application.credentials[:db][:username] || rails_dev %>
  password: <%= Rails.application.credentials[:db][:password] %>
  port: 5432
  pool: 5

development:
  <<: *default
  database: bookstore_develop

production:
  <<: *default
  database: bookstore_production

test:
  <<: *default
  host: <%= ENV['CIRCLECI'] ? 'localhost' : 'db' %>
  username: rails_dev
  database: bookstore_test
