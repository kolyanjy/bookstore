- if current_order.persisted?
  = link_to cart_path(session[:order_id]), {:class => 'shop-link pull-right visible-xs'} do
    %span.shop-icon
      %span.shop-quantity
        -# = current_order.decorate.sum_books
- if current_order.persisted?
  = link_to cart_path(session[:order_id]), {:class => 'shop-link pull-right hidden-xs'} do
    %span.shop-icon
      %span.shop-quantity
        -# = current_order.decorate.sum_books
