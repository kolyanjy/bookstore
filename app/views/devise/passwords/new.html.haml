%main.container.general-main-wrap
  = devise_error_messages!
  .text-center
    %h1.mb-30
      = t('questions.change?')
    %p.general-password-text
      = t('devise.reset_message')
  .general-form
    = simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
      .form-group.mb-25
        = f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder: t('devise.email')
      = f.submit t('devise.instructions'), class: "btn btn-default center-block mb-20"
      .text-center
        %a.in-gold-500.mb-20
          = link_to t('devise.cancel'), new_session_path(resource_name)
