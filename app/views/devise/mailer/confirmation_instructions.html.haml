%p
  = t('interpolation.wellcome', email: @resource.email)
%p
  = t('devise.confirmations.account_desc')
%p= link_to t('devise.confirmations.account'), confirmation_url(@resource, confirmation_token: @token)
